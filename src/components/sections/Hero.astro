<section id="hero">
  <div class="grid place-items-center backdrop-blur-sm transition-all min-h-screen">
    <div class="text-center">
      <p class="font-mono md:text-lg 2xl:text-xl" id="greeting">Hola 👋, mi nombre es</p>
      <h1 class="text-6xl md:text-7xl 2xl:text-8xl text-gradient font-bold tracking-tighter px-4" id="name">
        Esteban Florez
      </h1>
      <h2 class="text-slate-300 mt-1 uppercase font-bold tracking-wide md:text-lg 2xl:text-xl" id="title">Desarrollador Web</h2>
      <p class="mt-8 text-lg md:text-xl 2xl:text-2xl" id="hero-text">
        Construyo
        <span class="text-slate-200">aplicaciones</span>
        y
        <span class="text-slate-200">sitios web</span>
        <br>
        transformando
        <span class="text-green-400 font-fancy">ideas</span>
        a
        <span class="font-mono text-teal-400">código.</span>
      </p>
      <!-- TODO -> ver si poner esto o no -->
      <!-- <a href=""
        class="bg-gradient mt-4 text-slate-800 px-5 py-3 rounded-lg hover:brightness-90 flex gap-1 mx-auto w-max"
        id="hero-cta">
        Contáctame
      </a> -->
    </div>
  </div>
</section>
<style>
  #greeting {
    animation: appear .75s ease-in 0s, godown .75s ease-in 0s;
    animation-fill-mode: both;
  }
  
  #name, #title {
    animation: appear .75s ease-in .75s, godown .75s ease-in .75s;
    animation-fill-mode: both;
  }
  
  #hero-text {
    animation: appear .75s ease-in 1.5s;
    animation-fill-mode: both;
  }

  #hero {
    background-image: url('/src/images/bg-hero.svg');
    @apply bg-[20%] md:bg-[25%] lg:bg-left bg-cover;
  }

  @keyframes appear {
    0% {
      opacity: 0;
    }

    100% {
      opacity: 1;
    }
  }

  @keyframes godown {
    0% {
      transform: translateY(-2rem);
    }
    
    100% {
      transform: translateY(0);
    }
  }
</style>
