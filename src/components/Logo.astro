---
import { Image } from 'astro:assets'
const { src, title } = Astro.props
const image = import(`../images/logos/${src}.svg`)
---

<Image
  alt={`Logo de ${title}`}
  src={image}
  class="h-12 w-12 md:h-16 md:w-16 object-fill hover:brightness-125"
/>
